# Rapport — Task A2 : Bascule des endpoints ERA5 existants sur les marts + suppression des warmers (Lot 2)

Branche : `feat/climat-module`

## Réalisé

### 1. `/spi` et `/sti` → lecture directe `gold.fct_era5_indices_grid`

Plus de gamma-fit ni de z-score calculé côté API : la requête `SELECT era5_latitude,
era5_longitude, spi|sti FROM gold.fct_era5_indices_grid WHERE month=:m AND fenetre=:w`
est formatée par deux nouvelles fonctions pures testables sans DB
(`_rows_to_spi`/`_rows_to_sti` dans `observatory_era5.py`), qui arrondissent/classifient
via `classify_index` (réutilisé) — **shape de réponse identique**
(`[{latitude, longitude, spi|sti, index_class}]`), y compris le détail que `spi` est
arrondi à 3 décimales et `sti` ne l'est pas (comme avant).

Résolution du mois par défaut : nouvelle fonction `_resolve_indices_month` — `MAX(month)
FROM fct_era5_indices_grid WHERE fenetre=:w`, clampée si une date future/trop récente est
demandée (mirroring `_resolve_month_start`, mais scoping direct sur le mart au lieu de la
table journalière). Vérifié sur l'entrepôt réel : `MAX(month)` du mart = juin 2026 =
exactement le dernier mois complet calculé par l'ancienne logique sur `gold.era5_grid`
(juillet 2026 en cours au 2026-07-07).

### 2. `/monthly` → lecture directe `gold.fct_era5_monthly_grid`

Plus d'agrégation à la volée sur la table journalière. Point d'attention découvert en
vérifiant contre l'entrepôt réel : le champ historique `potential_evaporation` est une
**moyenne journalière négative** (convention brute ERA5, consommée telle quelle côté
front pour le bilan hydrique `total_precipitation + potential_evaporation`), alors que
`etp_totale` du mart est un **total mensuel positif**. Reconstruit avec
`-(etp_totale / nb_jours)` — vérifié bit-for-bit contre l'ancienne requête sur un mois
réel (Tours, mai 2026) : `-5.9177144881217711` (ancien) vs `-5.9177144881217711`
(reconstruit). `temperature_2m`/`total_precipitation` matchent aussi exactement
(`temperature_moyenne`/`precipitation_totale`).

### 3. `/snapshot` simplifié

Vérifié que `bronze.era5_france_timeseries` (vue `gold.era5_grid`) n'a **aucun doublon
de coordonnées** à précision flottante (`count(DISTINCT latitude) = count(DISTINCT
round(latitude,1))`, testé sur juin 2026 et juin 2015) — la fusion pondérée `AVG(...) +
GROUP BY round(...)` est donc devenue une opération identité, remplacée par une simple
projection avec arrondi défensif (pas de `GROUP BY`).

### 4. Warmers supprimés (`api/main.py`)

`_warm_era5_climatology` (gather initial + boucle de re-warm 6 jours), `CLIMATOLOGY_REWARM_SECONDS`,
import `delete_cached` (devenu inutile) — supprimés. Le lock single-flight
`_climatology_lock`/`_precip_climatology_lock` est **conservé** car `/anomaly` (chemin
température) reste un consommateur du scan ~71s à la demande (voir §5).

### 5. `/anomaly` conservé, `/temp-anomaly` supprimé

Grep frontend (`frontend/src` : `era5.anomaly`, `useERA5Anomaly`, `temp-anomaly`,
`tempAnomaly`) :
- **`/anomaly` est activement consommé** : `ObservatoryPage.tsx` (`useERA5Anomaly('temperature', ...)`),
  `RightDrawer.tsx` (radio `anomaly`), `Era5Banner.tsx` (`ANOMALY_VARIABLES=['anomaly']`),
  `ObservatoryMap.tsx` (rendu carte + popup + légende). L'audit du plan supposait cette
  variable orpheline côté UI — **c'est faux à l'état actuel du code** ; elle ne le
  deviendra qu'après Task C1 (retrait front). Endpoint **conservé** intact (branches
  température ET précipitation, cette dernière non appelée par le front actuellement
  mais laissée en l'état — pas dans le périmètre de cette tâche de la retirer).
- **`/temp-anomaly` n'a aucun client** : `observatory-api.ts` n'expose pas de méthode
  `tempAnomaly`/`temp-anomaly`, confirmé par grep vide sur tout `frontend/src`.
  **Supprimé** (endpoint + son usage dans `_compute_temp_anomaly`'s docstring nettoyé).

Code mort supprimé de `observatory_era5.py` (n'étaient utilisés que par l'ancien `/spi`/
`/sti`/les warmers, maintenant remplacés) : `_mom_gamma`, `SPI_MIN_REF_YEARS`,
`_era5_sti_reference`, `_era5_spi_reference`, `STI_WARM_WINDOWS`,
`_scan_monthly_cell_stats`, `_sti_ref_from_cells`, `_spi_ref_from_cells`,
`_warm_era5_sti_default`, `_warm_era5_spi_default`, `_sti_reference_lock`,
`_spi_reference_lock`. Dans `era5_anomaly.py` : `compute_sti`, `compute_spi_grid`
(gamma-fit + CDF, devenus morts) — `classify_index`, `compute_anomalies`,
`compute_precip_anomalies`, `window_end_months`, `add_months`, `latest_complete_month`
**conservés** (toujours consommés par `/anomaly` et/ou `observatory_climat.py`).
Net : **-556 lignes** sur les 3 fichiers backend touchés.

### 6. SPI station (`observatory_piezo.py` / `observatory_hydro.py`)

`/stations/{code}/spi` ne fait plus de fit gamma par station (`compute_spi` de
`dashboard/utils/drought.py`, retiré des imports) : nouvelle jointure
station → cellule mappée → marts :

```sql
SELECT m.mois, m.precipitation_totale AS value, g.spi
FROM gold.int_station_era5_mapping map          -- ou int_hydro_station_era5_mapping / code_station
JOIN gold.fct_era5_monthly_grid m
  ON m.era5_latitude = map.era5_latitude AND m.era5_longitude = map.era5_longitude
LEFT JOIN gold.fct_era5_indices_grid g
  ON g.era5_latitude = map.era5_latitude AND g.era5_longitude = map.era5_longitude
 AND g.month = m.mois AND g.fenetre = :window
WHERE map.code_bss = :code
ORDER BY m.mois
```

Nouveau paramètre `window` (défaut **1**, matching l'ancien comportement — le fit gamma
par station opérait sur les totaux mensuels bruts, pas une accumulation sur plusieurs
mois). Formatage extrait en fonction pure partagée `station_spi_rows` (nouvelle,
`api/era5_anomaly.py`), réutilisée à l'identique par les deux routers.

Shape de réponse (`{mois, value, spi, classification}`) **inchangée**. Les *valeurs*
changent légèrement pour les stations dont l'historique remonte avant les corrections
amont de coordonnées ERA5 (ex. `BSS004NKHH/X`, févr. 2025 : ancien `18.3065mm` vs
nouveau `45.0611mm` depuis le mart corrigé) — attendu et voulu : le mart est désormais
la source canonique, remplaçant l'ancienne agrégation station par station qui héritait
de l'imprécision pré-fix. Vérifié end-to-end sur l'entrepôt réel (piézo `BSS004NKHH/X`,
919 mois de jointure ; hydro `A021005050`, jointure correcte).

Le seuil `MIN_MONTHS=60` (5 ans mini pour un fit stable) de l'ancien
`compute_spi`/`_compute_gamma_index` **disparaît** : le SPI est désormais calculé sur la
référence climatologique complète du mart (1991-2020), indépendamment de la longueur de
l'historique de la station — amélioration attendue de la migration, pas une régression.

## Tests

- **`tests/test_era5_spi.py`** réécrit : tests DB-free pour `_rows_to_spi`,
  `_rows_to_sti`, `_resolve_indices_month` (remplace les anciens tests de
  `compute_spi_grid`/`_mom_gamma`/`_sti_ref_from_cells`/`_spi_ref_from_cells`, tous
  supprimés avec le code qu'ils testaient).
- **`tests/test_era5_anomaly.py`** : retrait des 8 tests de `compute_sti` (fonction
  supprimée) ; le reste (classify_index, compute_anomalies, compute_precip_anomalies,
  helpers de dates) inchangé et toujours vert.
- **`tests/test_station_spi.py`** (nouveau, 5 tests) : `station_spi_rows` — shape
  nominale, spi manquant (LEFT JOIN miss) → `classification=UNKNOWN`, value nulle
  préservée, tri par mois, entrée vide.
- `tests/test_era5_resolve_month_start.py` inchangé (toujours utilisé par `/anomaly`).

### Exécution

Sous-ensemble ciblé (ERA5/climat/piezo/hydro/station-SPI) :
```
PYTHONPATH=. DEBUG=true .venv/bin/python3 -m pytest tests/test_era5_spi.py \
  tests/test_era5_anomaly.py tests/test_era5_resolve_month_start.py \
  tests/test_station_spi.py tests/test_observatory_climat.py \
  tests/test_observatory_hydro_units.py tests/test_observatory_siblings.py \
  tests/test_situation_router.py tests/test_schemas.py tests/test_api_health.py -q
```
→ **127 passed**.

Suite élargie (tout `tests/` sauf ML training/pastas/pumping_detection — mêmes
exclusions que le rapport A1, précédent documenté : ces tests spawn des entraînements
réels de plusieurs minutes, sans rapport avec ce changement — et deux fichiers qui
bloquent indéfiniment dans cet environnement, `test_api_account_deletion.py` /
`test_api_admin_security.py`, **confirmés bloquants aussi sur le code AVANT modification**
via `git stash`) :
```
PYTHONPATH=. DEBUG=true .venv/bin/python3 -m pytest tests/ -o addopts="" -q \
  --ignore=tests/pastas --ignore=tests/pumping_detection --ignore=tests/auth \
  --ignore=tests/test_api_training.py --ignore=tests/test_api_forecasting.py \
  --ignore=tests/test_api_models.py --ignore=tests/test_api_pastas.py \
  --ignore=tests/test_api_explainability.py --ignore=tests/test_api_counterfactual.py \
  --ignore=tests/test_api_account_deletion.py --ignore=tests/test_api_admin_security.py
```
→ **211 passed, 3 failed** (`test_brgm_sectors_parse.py`,
`test_sectors_endpoint.py`, `test_sectors_timeline_endpoint.py`) — **les 3 échecs sont
préexistants et sans rapport** : reproduits à l'identique sur le code d'avant (`git
stash`), causés par une résolution DNS `brgm-postgres` (hostname docker, pas
`localhost`) et par un fichier de fixture local absent
(`/tmp/bdrv/wfs_2026-05-01.json`) — confirmé en relançant avec
`BRGM_DB_HOST=localhost BRGM_DB_PORT=49502` : 2 des 3 passent alors, le 3ᵉ reste
bloqué uniquement par le fichier manquant, sans lien avec cette tâche.

## Perf — avant/après

Ancienne implémentation (code supprimé) : scan complet ~71s (température) / ~130s
(précipitation, période 1991-2020) sur la table journalière `gold.era5_grid`, avec
warmers pour masquer ce coût — documenté dans le code avant suppression
(`api/main.py:84-159`, commentaires `~71s`/`~130s`).

Nouvelle implémentation, mesurée en direct contre l'entrepôt réel
(`BRGM_DB_HOST=localhost BRGM_DB_PORT=49502`, même méthode que A1) :

| Endpoint | Requête | Temps |
|---|---|---|
| `/spi` (window=3, dernier mois) | `fct_era5_indices_grid`, 11 496 cellules | **87.9 ms** (1ᵉʳ appel), 45.0 ms (2ᵉ) |
| `/sti` (window=3, dernier mois) | idem | **41.4 ms** |
| `/monthly` (mai 2026) | `fct_era5_monthly_grid`, 11 496 cellules | **473.1 ms** (vs 705 ms ancien, mesuré comparativement) |
| `/snapshot` (dernier jour) | `gold.era5_grid`, simplifié | **139.7 ms** |

Aucun cache Redis nécessaire pour ces temps — **sub-seconde dès le premier appel**,
confirmant l'objectif du plan (vs 71s pour l'ancien chemin gamma-à-la-volée). `/anomaly`
(chemin température, conservé) reste sur l'ancien scan ~71s à froid — non re-mesuré
(code inchangé), simplement plus pré-chauffé au démarrage.

## Commit

`perf(api): endpoints ERA5 sur les marts matérialisés — suppression des warmers 71 s et
du calcul à la volée`

## Points d'attention pour la suite

- **`/anomaly` reste vivant** — sa suppression est repoussée à Task C1 (qui retire la
  variable front `anomaly` en même temps). Tant que C1 n'est pas fait, un cache-miss sur
  `/anomaly` (température) déclenche toujours le scan ~71s (protégé par le lock
  single-flight, mais plus jamais pré-chauffé).
- Le chemin précipitation de `/anomaly` (`variable=precipitation`) n'est appelé par
  aucun code front actuel (seul `variable='temperature'` est utilisé) — laissé en l'état,
  hors périmètre de cette tâche.
- Valeurs de `/stations/{code}/spi` (piezo + hydro) : changent légèrement vs avant pour
  les mois antérieurs aux corrections amont de coordonnées ERA5 — attendu (le mart est
  désormais la source canonique). À signaler si un utilisateur remarque un changement
  d'historique SPI sur une station piézométrique/hydrométrique donnée.
- `junon-backend-dev` non re-testé en HTTP réel dans ce run (même limitation
  documentée en A1 — rebuild Docker indisponible dans cet environnement) ; vérification
  faite via appel direct des fonctions/requêtes contre l'entrepôt réel.
