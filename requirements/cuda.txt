-r base.txt
# NOTE: This file is a legacy convenience list and is NOT used by the real
# build. The authoritative sources are pyproject.toml (extra `cuda`) + uv.lock.
# Docker (docker/backend/Dockerfile) and CI (uv sync) install from those.
# PyTorch CUDA 12.6 (host is CUDA 12/13, not 11.8)
# Note: Install PyTorch separately with:
# pip install torch torchaudio torchvision --index-url https://download.pytorch.org/whl/cu126
torch>=2.5.0
torchaudio>=2.5.0
torchvision>=0.20.0
